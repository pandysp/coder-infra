name: Build workspace image

on:
  push:
    branches: [main]
    paths:
      - 'images/**'
  schedule:
    - cron: '0 6 * * 1'
  workflow_dispatch:

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: pandysp/coder-workspace

jobs:
  build:
    name: Build and push
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - uses: actions/checkout@v4

      - uses: docker/setup-buildx-action@v3

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: pandysp
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          context: images/workspace
          platforms: linux/amd64
          push: true
          tags: |
            ghcr.io/pandysp/coder-workspace:latest
            ghcr.io/pandysp/coder-workspace:${{ github.sha }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Smoke test
        env:
          IMAGE_TAG: ghcr.io/pandysp/coder-workspace:${{ github.sha }}
        run: |
          docker run --rm "$IMAGE_TAG" bash -c "
            mise --version && pnpm --version && docker --version && tailscale --version &&
            rg --version | head -1 && fd --version | head -1 && gh --version | head -1 &&
            echo '✓ All tools present'
          "
