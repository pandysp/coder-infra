# This file is not used directly.
# The provision.sh script generates a temporary inventory at runtime.
#
# To run Ansible manually against an existing server:
#
# [coder]
# coder-dev ansible_user=root
